# Copy to .env and fill values. Do NOT commit real .env to git.
BITRIX_WEBHOOK=https://your-domain.bitrix24.ru/rest/1/SECRET_TOKEN/crm.lead.add
PORT=3000
# Comma-separated allowed origins for CORS (leave empty to allow all)
ALLOWED_ORIGINS=http://localhost:3000
